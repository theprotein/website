block('page').elem('js')(
    def()(function() {
        var elem = this.elem,
            scripts = [];

        scripts.push(
            '<!--[if lt IE 9]>',
            { elem : elem, url : '//yastatic.net/es5-shims/0.0.1/es5-shims.min.js' },
            '<![endif]-->',
            { elem : elem, url : '//yastatic.net/jquery/2.1.3/jquery.min.js' },
            { elem : elem, url : 'index.min.js' }
        );

        return scripts;
    })
);
