.paragraph
{
    font-family: 'Roboto', sans-serif;
    font-weight: 300;
    color:       #333;
    line-height: 1.6rem;
    font-size:   remify(16px);

    &::selection
    {
        background: #FCC961;
    }
}
