block('technology')(
    content()(function() {
        this.ctx.icon && (this.ctx.icon.mix = { block: this.block, elem: 'icon' });
        return [
            this.ctx.icon,
            {
                elem: 'text',
                content: this.ctx.text
            }
        ];
    }),
    elem('sup').tag()('sup')
);
